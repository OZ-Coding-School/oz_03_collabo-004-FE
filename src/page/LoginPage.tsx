import AuthComponent from "../components/AuthComponent";

const LoginPage = () => {
    return (
        <>
            <AuthComponent type="LOGIN" />
        </>
    );
};

export default LoginPage;
